# CARG API endpoint (for live evaluation)
CARG_API_BASE=https://danmonteiro.com/api

# Anthropic API key (for LLM-as-judge scoring)
# When unset, all scoring falls back to rule-based heuristics
ANTHROPIC_API_KEY=
